{
  "name": "playground",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rimraf dist && cd ../frontend && npm run build && cp -r dist ../Playground",
    "deploy:full": "git add . && git commit -m uibuild && git push"
    
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cros": "^1.1.0",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
